import React, { Component } from 'react'

export default class Settings extends Component {
    render() {
        return (
            <div className="container already-logged-in">
                <h1> Funktion "Einstellungen" noch nicht verfügbar</h1>
            </div>
        )
    }
}
